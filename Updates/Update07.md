---
status: Done
---

- Rendering still not working in Live Preview SOMETIMES.  It mostly works but this text is breaking the rendering in Live Preview: "$X \sim N(\mu, \sigma^2)$".  So perhaps the problem is with special characters.
- Please re-format main.js.  The entire file is on two lines.  It should follow standard conventions for a .js file.
- Let's build a display for the right sidebar.  It should list all instances of each Identifier used in the vault.  Sort alphabetically by parent/child.  Clicking the Identifier should take you to the file and line where it is used.


# Claude output
...
